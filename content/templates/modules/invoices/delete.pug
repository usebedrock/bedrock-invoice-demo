extends index

block append pageVariables
    - var pageTitle = "Delete invoice"

block content

    p Are you sure you want to delete this invoice?
    .btn-toolbar
        a.btn.btn-link(href="#") Cancel
        a.btn.btn-primary(href="#") Delete invoice